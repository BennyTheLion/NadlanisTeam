.claude/

# תוכן שנוצר בזמן ריצה (תמונות שהועלו) — לא חלק מהקוד, מגיע לפרודקשן דרך העלאה
# ישירה באתר עצמו, לא דרך git. .htaccess כן נשאר במעקב (כבר היה קיים).
/uploads/*
!/uploads/.htaccess

# פרטי DB אמיתיים של השרת — לעולם לא ל-git, ראו includes/db-config.example.php
/includes/db-config.php
